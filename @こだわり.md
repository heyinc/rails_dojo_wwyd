# 課題1: 注文情報の作成と決済処理 実装のこだわりポイント

今回の実装では、単に機能要件（注文・決済・在庫更新）を満たすだけでなく、商用環境での運用を想定した**「データの整合性」「障害耐性」「保守性」**の3点に特にこだわって実装を行いました。

## 1. データの整合性と排他制御 (Data Integrity & Concurrency Control)

### 悲観的ロックによる在庫管理
ECサイト等の注文処理で最もクリティカルな問題の一つである「在庫のオーバーセル（売り越し）」を防ぐため、`ActiveRecord` の悲観的ロック（Pessimistic Locking）を導入しました。

```ruby
locked_item = Item.lock.find(item.id)
raise "商品の在庫がありません" if locked_item.stock < 1
```

これにより、在庫チェックから更新までの間に他のリクエストが割り込んで在庫を奪う競合状態（Race Condition）を確実に防止しています。

### トランザクション管理
注文作成、決済情報の保存、予約ステータスの更新、在庫の減算という複数のDB操作を `ActiveRecord::Base.transaction` ブロックで囲むことで、アトミック性を保証しています。決済API呼び出しも含めることで、万が一API後にDB保存で失敗した場合でも、データ不整合が起きないよう設計しました（※決済API自体の二重決済リスク等は、API側の冪等性担保が必要なため今回はDB整合性を優先）。

## 2. 厳格な例外ハンドリング (Strict Error Handling)

「予期せぬエラーはユーザーに見せず、システムエラーとして扱う」という原則に従い、`rescue` 節の範囲を厳格に制御しました。

- **`ActiveRecord::RecordInvalid`**:
  バリデーションエラーはユーザーが修正可能なエラーであるため、入力画面を再表示します。
- **`Timeout::Error`**:
  外部システム（決済API）の障害を想定し、専用のメッセージ（「決済処理がタイムアウトしました...」）を表示してリトライを促します。
- **`RuntimeError` (Business Logic Errors)**:
  `rescue => e` で全ての例外を捕捉する「握り潰し」実装を避け、意図的に発生させたビジネスエラー（"商品の在庫がありません"）のみをフィルタリングして捕捉しています。
  
  ```ruby
  rescue RuntimeError => e
    if e.message == "商品の在庫がありません"
      # ...画面再表示...
    else
      raise e # 想定外のバグ（NoMethodError等）は隠蔽せず500エラーにする
    end
  end
  ```

これにより、開発中のバグや予期せぬシステム障害が「在庫切れです」のような誤ったメッセージでマスクされることを防ぎます。

## 3. コードの保守性と可読性 (Maintainability & Readability)

### DRY (Don't Repeat Yourself) 原則の適用
`new` アクションと `create` アクション（のエラー時）で共通して必要なフォーム用データの取得処理を、プライベートメソッド `prepare_form_data` に切り出しました。

```ruby
  def prepare_form_data
    @items = Item.all.order(:name)
    @users = User.all.order(:name)
  end
```

これにより、将来的に選択肢の取得ロジックが変更された場合（例：在庫ありの商品のみ表示するなど）も、一箇所の修正で対応可能です。

### ガード節 (Guard Clause) の活用
予約ステータスのチェックなどを処理の冒頭でガード節として記述することで、ネストを浅く保ち、メインロジックの可読性を高めています。

```ruby
    if reservation.status == "completed"
      # ...エラー処理...
      return
    end
```

# 課題2: 注文キャンセル機能 実装のこだわりポイント

課題2では、作成された注文を取り消す（Undo）機能を実装しました。ここでも「データの整合性」が最重要課題となりました。

## 1. デッドロックの回避 (Deadlock Avoidance)

注文キャンセル処理（`destroy`）は、注文作成処理（`create`）の逆操作を行います。この際、複数のリソース（`Item`と`Reservation`）に対してロックを取得する必要がありますが、**ロックの取得順序**を誤ると、システム全体が停止するデッドロック（Deadlock）を引き起こすリスクがありました。

このリスクを回避するため、`destroy` アクションにおけるロック取得順序を、`create` アクションと完全に一致させました。

**Create Action:**
1. `Item` Lock (via `Item.lock.find`)
2. `Reservation` Lock/Update (Implicitly locked during update)

**Destroy Action:**
```ruby
    ActiveRecord::Base.transaction do
      # Prevent deadlock: Lock order must match create action (Item -> Reservation)
      item.lock!        # 1. Item Lock
      reservation.lock! # 2. Reservation Lock

      reservation.update!(status: "pending")
      item.update!(stock: item.stock + 1)

      order.destroy!
    end
```

このように、常に **Item -> Reservation** の順序でリソースにアクセスすることで、高負荷時でもデッドロックが発生しない安全な設計としています。

## 2. アトミックなUndo操作

キャンセル処理は、「在庫を戻す」「予約ステータスを戻す」「注文履歴を消す」という3つの操作がセットで完了しなければなりません。
これらを単一のトランザクション内で実行することで、例えば「在庫は戻ったが注文履歴が消えていない」といったデータ不整合が絶対に起きないよう保証しています。

---

以上の工夫により、高負荷時や異常系でもデータが壊れにくく、かつ開発者が安心してメンテナンスできるコードベースを目指しました。
